# CRE Outbound Engine -- Paused / Private

**Status:** Intentionally removed from the public site. Not discarded.

**Why it came down:** The page is internship-adjacent content that isn't ready for a public audience. The build is real and the workflow works, but the context around it needs more care before it belongs in the Library.

**What's here:** `cre-outbound-engine.html` in this folder is the full article as it existed when it was live. All content is preserved.

**To republish:** Move `cre-outbound-engine.html` to `public/library/`, add the redirect rule back to `netlify.toml`, and restore the related card in `meeting-notes-to-airtable.html`.
